# Codex History Viewer 用户体验（UX）优化方案与待办记录

> 记录日期：2026年9月  
> 目标：在保证高性能与本地优先（Local-First）原则的前提下，针对日常浏览、代码落地、多会话管理与检索等真实场景，全面提升扩展的用户体验。

---

## 目录

- [一、背景与现状](#一背景与现状)
- [二、体验优化维度与具体方案](#二体验优化维度与具体方案)
  - [维度一：会话阅读与长对话导航（核心痛点）](#维度一会话阅读与长对话导航核心痛点)
  - [维度二：IDE 与工作区深度联动（开发利器）](#维度二ide-与工作区深度联动开发利器)
  - [维度三：侧边栏与会话管理体验（日常高频）](#维度三侧边栏与会话管理体验日常高频)
  - [维度四：搜索与检索体验（检索提效）](#维度四搜索与检索体验检索提效)
  - [维度五：导出、分享与知识沉淀（协同沉淀）](#维度五导出分享与知识沉淀协同沉淀)
  - [维度六：键盘快捷键与交互细节（极客体验）](#维度六键盘快捷键与交互细节极客体验)
- [三、优先级与实施路线图](#三优先级与实施路线图)
- [四、涉及模块与技术参考](#四涉及模块与技术参考)

---

## 一、背景与现状

Codex History Viewer 在 2.13 ~ 2.15 版本中已完成单趟扫描、紧凑 JSON 序列化、Shiki 高亮 LRU 缓存、代码块视口懒高亮、会话模型内存 mtime 缓存等一系列扎实的底层性能优化，会话初次打开与日常刷新的速度已大幅改善。

然而，在日常实际使用中，随着历史会话的积累与长对话、复杂任务的增多，仍存在一些影响操作流程度和阅读舒适度的体验瓶颈：
1. **长会话迷失**：长达数十轮的问答与工具调用缺乏大纲与导航，定位历史提问只能盲翻。
2. **思考过程缺失**：Claude 3.7 / Extended Thinking 的思考过程（`thinking` 块）在提取时被跳过，无法展开查看推理思路。
3. **代码落地割裂**：看到生成的代码或补丁，无法一键在编辑器打开或与本地文件进行 Diff 比对。
4. **工作区过滤不够顺手**：历史树缺乏一键只看当前项目的常驻开关，树视图不支持批量操作。

---

## 二、体验优化维度与具体方案

### 维度一：会话阅读与长对话导航（核心痛点）

#### 1.1 会话问答大纲 / 目录导航（TOC / Prompt Outline）[✅ 已完成]
- **现状痛点**：
  - 会话动辄数千行，用户想找“第 5 个问题”或“刚才讨论数据库架构的那一轮”，目前只能疯狂滚轮或全局 Ctrl+F 盲搜。
- **优化方案与实现效果**：
  - 在 Session Viewer 顶部工具栏增加 **“目录 / 大纲（TOC）”** 按钮 `[ ≡ ]`。
  - 点击弹出轻量侧滑抽屉，提取当前会话所有用户 Prompt：
    - 序号标识（如 `#1, #2, #3...`）；
    - 提问文本摘要与时间戳；
    - 代码修改标签（`diff` 标签提示当前提问产出了代码补丁）。
  - 点击任一项自动收起大纲抽屉，页面平滑滚动并闪烁高亮定位到对应消息，支持 `Esc` 键快捷关闭。
  - 同步补齐中、英、日三语本地化与深浅主题自适应。

#### 1.2 Claude 思考过程（Extended Thinking / Reasoning）折叠展示 [➖ 已评估跳过]
- **说明**：经实际体验评估，即使折叠呈现也较占阅读篇幅，按需恢复为原版纯净紧凑问答布局，不在会话中展示思考块。

#### 1.3 粘性问答指示器（Sticky Prompt Breadcrumbs）
- **现状痛点**：
  - 遇到 AI 生成长篇代码或跑了超长测试输出时，页面滚动数屏后脱离上下文，不知当前内容在回答什么。
- **优化方案**：
  - 在页面顶部增加轻量粘性指示条，滚动时显示当前回复所对应的用户 Prompt 截断标题，点击可瞬间回跳到提问位置。

---

### 维度二：IDE 与工作区深度联动（开发利器）[⏸️ 暂不实施]

#### 2.1 代码块增强：“在编辑器中打开”与长代码折叠 [⏸️ 暂不实施]
- **现状痛点**：
  - 目前代码块右上角仅有复制按钮。用户阅读长代码后若想格式化或使用，只能复制后手动切到编辑区新建文件粘贴。
- **优化方案**：
  - 代码块右上角新增 **“在编辑器中打开（Open in Untitled Editor）”** 图标，点击直接调用 `vscode.workspace.openTextDocument` 打开对应语言的临时文件。
  - 超过一定高度（如 80 行）的代码块增加折叠/展开开关，避免撑爆阅读流。

#### 2.2 Patch 补丁与工作区文件联动（Open File & Diff with Workspace）[⏸️ 暂不实施]
- **现状痛点**：
  - 用户在 Webview 中查看 Patch 补丁时只能看静态 Diff，无法快速跳转到对应的工作区本地文件。
- **优化方案**：
  - 在每个 Patch 条目头部增加 **“在编辑器中打开本地文件”** 按钮。
  - 增加 **“与工作区文件比对（Diff with Active File）”**：调用 VS Code 内置 `vscode.diff` 命令，将历史 Patch 的变更与本地当前文件做左右分栏对比。
  - 可选：支持“一键应用补丁（Apply Patch to Workspace）”。

---

### 维度三：侧边栏与会话管理体验（日常高频）[⏸️ 暂不实施]

#### 3.1 History 树“仅看当前工作区”常驻切换 Toggle [⏸️ 暂不实施]
- **现状痛点**：
  - 目前历史树过滤需要弹出多级 QuickPick 菜单，而 Pinned 树已有当前项目过滤。用户打开不同项目工程时，主要历史树混杂了大量无关项目会话。
- **优化方案**：
  - 在 History View 的视图标题栏直接增加 **“按当前工作区过滤（Toggle Current Workspace Only）”** 图标按钮。
  - 点击即可在“全部会话”与“仅当前项目”之间一键切换，并同步高亮图标状态（通过 `setContext` 驱动）。

#### 3.2 会话多选与批量操作（Batch Actions）[⏸️ 暂不实施]
- **现状痛点**：
  - 树视图目前只支持单选右键操作。批量清理空会话、批量打 Tag、批量导出时极其繁琐。
- **优化方案**：
  - 开启 TreeView 的 `canSelectMany: true`。
  - 支持多选执行：批量置顶/取消置顶、批量添加/移除标签、批量删除、批量导出。

#### 3.3 最近浏览会话快速切换（Quick Switcher）[⏸️ 暂不实施]
- **优化方案**：
  - 绑定快捷键（如 `Ctrl+Alt+H`），弹出最近浏览过的 10~15 个会话 QuickPick 列表，支持拼音/模糊匹配快速回跳。

---

### 维度四：搜索与检索体验（检索提效）

#### 4.1 搜索结果上下文高亮片段（Context Snippets）[✅ 已完成]
- **现状痛点**：
  - 搜索树中目前展示命中的单行或固定截断，上下文不足以判断是否为目标对话。
- **优化方案与实现效果**：
  - 优化搜索片段边界吸附逻辑（`buildAround`）：自动吸附到周围单词边界，避免生硬截断单词。
  - 搜索树节点命中词醒目高亮：使用 VS Code 原生 `TreeItemLabel.highlights`，在树节点中原生高亮命中关键词。
  - 搜索节点 Tooltip 与会话多项命中预览中，自动高亮匹配项（以加粗代码风格呈现）。

#### 4.2 搜索语法辅助标签（Filter Chips / Syntax Helper）[⏸️ 暂不实施]
- **现状痛点**：
  - 扩展支持 `model:xxx`、`source:claude`、`tag:xxx` 等语法，但用户难以记忆。
- **优化方案**：
  - 在搜索输入框上方或 QuickPick 预设常用的 Filter Chips：`[Codex]`、`[Claude]`、`[仅用户问题]`、`[含代码修改]`，点击即自动填入对应前缀。

#### 4.3 点击搜索结果平滑锚定与呼吸高亮 [⏸️ 暂不实施]
- **优化方案**：
  - 从搜索树点击进入 Webview 时，页面不仅自动滚动到对应行，还伴随 1~2 秒的黄色背景呼吸动画（Fade-out Pulse），提升视觉定位感。

---

### 维度五：导出、分享与知识沉淀（协同沉淀）

#### 5.1 纯净问答 Markdown 导出与一键复制（Clean QA Only）[✅ 已完成]
- **现状痛点**：
  - 现有的虚拟 Markdown / 导出包含大量 CLI 环境噪音、元数据与工具原生 JSON，不适合直接做知识库沉淀。
- **优化方案与实现效果**：
  - 核心渲染层增加纯净问答模式（`cleanQaOnly: true`）：彻底过滤系统提示词、协议环境信息、工具调用 JSON、终端执行输出等噪音，仅提取用户提问与 AI 核心回答、代码块。
  - 导出会话（`exportSessions`）默认提供“纯净问答 Markdown”选项，支持批量导出为 `.clean.md` 文件。
  - 大纲抽屉（TOC Header）内置一键复制纯净问答按钮 `[📋]`，点击即复制整场对话的纯净问答到剪贴板，开箱即贴入 Obsidian / Notion / 微信公众号。
  - 支持虚拟纯净 Markdown 文档打开与命令调用。

#### 5.2 单轮问答一键复制（Copy QA Pair）[⏸️ 暂不实施]
- **优化方案**：
  - 在每轮 User / Assistant 气泡右上角提供“复制本轮问答”按钮，格式化为标准 Markdown 格式存入剪贴板。

---

### 维度六：键盘快捷键与交互细节（极客体验）[⏸️ 暂不实施]

#### 6.1 Session Viewer 键盘操作增强 [⏸️ 暂不实施]
- **优化方案**：
  - 支持类 Vim / 极客阅读快捷键：
    - `J` / `K`（或 `Alt+Down` / `Alt+Up`）：跳到下一条 / 上一条消息；
    - `T` / `B`：跳到顶部 / 底部；
    - `/`：快速聚焦页内搜索框；
    - `Esc`：统一退出弹层/搜索；
    - `?`：弹出“快捷键速查指南（Shortcuts Cheat Sheet）”浮层。

---

## 三、实施路线图与最终执行状态

| 优先级 | 优化项 | 价值说明 | 最终状态 |
|:---|:---|:---|:---|
| **P0** | **会话问答大纲（TOC）** | 解决长会话阅读的最大痛点，查找历史提问立竿见影 | ✅ 已完成并推送（commit: `ed61dbb`） |
| **P0** | **Claude Thinking 思考链折叠展示** | 补齐现代推理模型的关键内容展现能力，提升专业感 | ➖ 经评估占地，按需求恢复为不显示 |
| **P2** | **纯净 Markdown 导出与一键复制** | 过滤环境与工具噪音，便于技术总结与团队分享，知识沉淀利器 | ✅ 已完成 |
| **P3** | **搜索结果上下文 Snippet 与高亮** | 自动吸附词边界并在树节点/Tooltip原生高亮命中词、结果默认展开 | ✅ 已完成 |
| **P1** | **代码块“在编辑器中打开” + Patch 本地文件联动** | 深度打通 VS Code 原生能力，消除复制粘贴 | ⏸️ 暂时搁置（暂不实施） |
| **P1** | **History 树“仅看当前工作区”常驻 Toggle** | 解决多项目历史混杂问题，日常使用频率极高 | ⏸️ 暂时搁置（暂不实施） |
| **P2** | **会话多选与批量操作（打标签/置顶/删除）** | 大幅降低历史会话整理和归档的时间成本 | ⏸️ 暂时搁置（暂不实施） |
| **P2** | **单轮问答一键复制（Copy QA Pair）** | 方便单轮问答快速拷给同事或记录 | ⏸️ 暂时搁置（暂不实施） |
| **P3** | **常用搜索语法 Filter Chips（辅助快捷标签）** | 降低复杂搜索门槛，辅助快速查询 | ⏸️ 暂时搁置（暂不实施） |
| **P3** | **粘性问答指示条（Sticky Prompt Breadcrumbs）** | 消除长代码/长输出阅读时丢失提问上下文问题 | ⏸️ 暂时搁置（现有配置支持固定提问） |
| **P3** | **键盘快捷键与 `?` 帮助速查面板** | 提升重度键盘用户的极客体验 | ⏸️ 暂时搁置（暂不实施） |

---

## 四、涉及模块与技术参考

1. **Webview 时间线与前端渲染**：
   - 核心文件：`media/chatView.js`、`media/chatView.css`
   - 宿主管理：`src/chat/chatPanelManager.ts`、`src/chat/chatModelBuilder.ts`
2. **Claude 消息提取与思考链**：
   - 核心文件：`src/chat/chatAttachments.ts`（`extractClaudeMessageContent`）
3. **侧边栏树视图与工作区过滤**：
   - 核心文件：`src/tree/historyTree.ts`、`src/extension.ts`、`package.json`
4. **代码块与 Patch 联动**：
   - 宿主命令：`vscode.workspace.openTextDocument`、`vscode.diff`
